package by.maxluxs.crypto2893.utils

import androidx.navigation.ui.AppBarConfiguration
import by.maxluxs.crypto2893.R

object AppBarConfigurations {

    val mainAppBarConfiguration = AppBarConfiguration(
        setOf(
            R.id.currencyListDestination,
            R.id.converterDestination
        )
    )
}